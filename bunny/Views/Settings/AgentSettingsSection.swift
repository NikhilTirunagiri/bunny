import SwiftUI

/// Placeholder until plan B's Task 7 replaces the body with the real Agents section
/// (default agent, CLI paths with detect + status, autonomy, open sessions in, default workspace).
struct AgentSettingsSection: View {
    var body: some View {
        ContentUnavailableView(
            "Agents",
            systemImage: "sparkle",
            description: Text("Connect Claude Code and Codex here.")
        )
    }
}
